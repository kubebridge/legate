# Normalize line endings so Fantomas and CI see the same bytes on every OS.
* text=auto eol=lf

# Shell scripts must keep LF line endings.
*.sh text eol=lf

# Windows-only scripts keep CRLF.
*.ps1 text eol=crlf
*.cmd text eol=crlf
